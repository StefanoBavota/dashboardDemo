export interface ClientFilters {
  search: string,
  born: number,
  cardYear: number,
  active: string
}

export interface PaymentFilters {
  search: string,
  area: string,
  client: string,
  date: string
}
